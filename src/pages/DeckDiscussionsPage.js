import { Layout } from 'antd';

function DeckDiscussionsPage() {
    return (
        <Layout style={{ 
            flex: 1,
            padding: 24,
            maxHeight: '100%',
            overflow: 'auto',
        }}>
            <p>DECK DISCUSSIONS PAGE</p>
        </Layout>
    )
}

export default DeckDiscussionsPage;