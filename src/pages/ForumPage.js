import { Layout } from 'antd';

function ForumPage() {
    return (
        <Layout style={{ 
            flex: 1,
            padding: 24,
            maxHeight: '100%',
            overflow: 'auto',
        }}>
            <p>FORUM PAGE</p>
        </Layout>
    )
}

export default ForumPage;