import { Layout } from 'antd';
import React from 'react';

function HomePage() {
    return (
        <Layout style={{ 
            flex: 1,
            padding: 24,
            maxHeight: '100%',
            overflow: 'auto',
        }}>
        </Layout>
    )
}

export default HomePage;